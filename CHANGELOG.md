# 更新记录

## 1.0.6 - 2026-07-16

### 编辑与预览体验

- 编辑区首次打开默认使用“精简”模式；用户手动切换到完整或精简模式后，会话内继续保留该选择。
- 工具栏“解包格式化”更名为“JSON字符串格式化”，功能行为保持不变。
- 主工作区默认调整为编辑区 / 预览区 `4:6` 分栏，让预览获得更充足的阅读空间。
- 树形预览新增路径显示开关，仅树形模式可见；路径默认显示，隐藏后会跨新 tab 保留该展示偏好，不影响路径复制、搜索和选中节点。
- 文本预览中的对象和数组在括号右侧显示 `n个字段` 或 `n项`；展开、折叠和空容器均支持，标签尺寸针对单行阅读进一步优化。

### 稳定性与测试

- 新增编辑模式默认策略、跨 tab 路径偏好、默认分栏比例及对象/数组计数协议的回归覆盖。
- 保持现有展开层级、搜索、长文本横向滚动、构建产物与发布校验链路通过。

## 1.0.5 - 2026-05-13

### 重点更新

- 预览搜索：搜预览时搜索框旁的 `n/m` 与上下键改为按「每个分支最浅一层命中」导航，避免子节点 `path` 继承父键名（例如搜 `ztbxxinfo` 时整棵数组子行都算命中）导致计数膨胀、下一命中误跳进子行的问题；树/文本行高亮与命中链自动展开仍使用全量命中集合，视觉反馈不变。
- 预览区底部栏在「复制路径」左侧新增「滚到顶」「滚到底」：垂直快速定位 `#previewContent` 虚拟列表视口；不改动 JSON、展开态与选区，横轴滚动保持不动以免打断阅读超长行。
- 上述滚动按钮采用轻量胶囊样式，中文标签在左、箭头在右（↑ / ↓），与纯文字复制按钮区分层次。

### 稳定性与测试

- 抽取 `preview-search-policy.js` 承载浅层命中筛选逻辑，并新增对应单元测试，避免主线程 `app.js` 与测试口径漂移。

### 发布工程化

- 项目改造成 npm + Vite + CRXJS 的 Manifest V3 构建工程，`package.json` 成为版本唯一来源；构建产物位于 `dist`，可直接作为 Chrome 扩展加载。
- 新增版本同步、发布校验、版本说明提取和构建产物校验脚本，阻断版本文件、tag 或更新记录不一致的发布。
- 新增 GitHub Actions：PR 与 main 推送执行静态检查、测试和构建；推送 `vX.Y.Z` tag 后自动生成 `json-prism-deck-vX.Y.Z.zip`、SHA-256 并发布 GitHub Release。

## 1.0.4.1 - 2026-05-12

### 重点更新

- “复制值”在叶子字符串场景下不再带最外层引号：选中 `"2026-01-12"` 这样的字符串节点，复制结果直接是裸值 `2026-01-12`，方便粘贴到表单、URL、SQL 或 IDE 字符串里，省掉一步手动删引号。
- 容器节点（对象/数组）与其他原始类型（数字/布尔/null）保持原行为，仍按合法 JSON 序列化，确保“复制子树粘贴回接口调试”的链路不被影响。

## 1.0.4 - 2026-04-20

### 重点更新

- 右侧功能栏宽度由 `192px` 调整为 `184px`，在不改移动端分支规则的前提下，给主工作区让出更多横向空间。
- 预览区新增“展开层级”按钮与下拉菜单，按当前 JSON 深度动态提供 `展开层级1..N` 选项；选中后按钮会回填当前层级。
- 选中“展开层级N”时会展开深度 `0..N` 的容器节点，更深层级保持折叠；手动点单节点展开/折叠时，不会清空已选层级文案。
- “展开全部 / 折叠全部 / 贴新数据默认全展开”保持更高优先级，触发时会统一重置层级按钮为“展开层级”。

### 修复

- 修复示例数据下“展开层级3”和“展开层级4”效果一致的问题：层级选项上限现在按“可展开容器最大深度”计算，不再出现选了也无变化的无效档位。

### 稳定性与测试

- 新增层级展开策略测试，覆盖选项生成、按层级展开、优先级重置、手动展开保留、搜索优先等关键场景。
- 全量测试持续通过，确认这轮改动未破坏既有的树形预览、文本预览、排序重建和解包流程。

## 1.0.3 补充 - 2026-04-09

### 重点更新

- 预览模式分段按钮顺序调整为 `文本 / 树形 / 元数据`，把文本入口前置。
- 预览区首屏默认选中 `文本`，并在每次打开或刷新页面时都强制回到文本预览。
- 会话恢复时不再沿用上一轮临时切换的预览模式，避免刷新后落回树形或元数据造成入口不一致。

### 稳定性与测试

- 新增预览模式默认策略回归测试，覆盖“按钮顺序”“首屏激活态”“恢复状态时强制文本模式”三个场景。
- 保持现有测试套件通过，确保这次入口层改动没有影响树形渲染、排序重建和字符串解包链路。

## 1.0.3 补充 - 2026-03-25

### 重点更新

- 修复树形预览里超长字符串会被提前截成 `…` 的问题，长 URL、长 token、长查询串现在会完整显示。
- 继续沿用预览区现有的横向滚动和右侧路径固定方案，超长值改为通过横向滚动查看全文，不再依赖摘要截断。
- 预览搜索改为基于完整字符串内容参与命中，88 个字符之后的片段也可以被正常搜到。

### 稳定性与测试

- 新增树形预览回归测试，覆盖“长字符串预览保留全文”和“搜索语料不再是省略号摘要”两个场景。
- 保持现有排序切换与字符串 JSON 解包测试通过，确保这次展示层修复没有影响既有解析与重建链路。

## 1.0.3 补充 - 2026-03-24

### 重点更新

- 修复排序切换后“源顺序”可能无法稳定恢复的问题：切回源顺序时会强制按编辑区当前文本重解析，并为重建链路补了结果版本保护。
- 粘贴新数据时，编辑区与预览区滚动条会同时复位到顶部/最左；格式化/压缩回写时只复位编辑区，预览区位置保持不变。
- 预览区头部控件整体缩小，状态文案与标题同排显示，进一步释放内容区高度。
- 预览区底部“当前节点”栏改为单行轻量信息：移除当前值展示，复制按钮改为小号无边框文本样式。
- 工具栏去掉 `解包压缩`，保留 `解包格式化`；功能语义由“解包格式化 + 常规压缩”覆盖。

### 稳定性与测试

- 新增排序切换策略测试，覆盖“切回源顺序必须重解析”“派生排序可复用快照”“文本漂移需重解析”三类场景。
- 保持现有解包测试与语法检查通过，确保这轮交互调整未破坏核心解析链路。

## 1.0.3 - 2026-03-23

### 重点更新

- 工具栏新增 `解包格式化`、`解包压缩` 两个按钮，专门处理“根节点是字符串、字符串内部仍然是一份 JSON”的数据。
- 工具栏里的 `压缩 / 下载 / 复制 / 导入` 收成两列紧凑小按钮组，减少右侧纵向占用。

### 字符串 JSON 解包

- 新增显式解包逻辑：只有用户点击解包按钮时，才会把包装字符串改写成正常的对象/数组 JSON。
- 常规 `格式化 / 压缩` 继续保持“输入是什么就处理什么”的语义，不会自动篡改普通字符串数据。
- 当当前内容不属于可解包的 JSON 字符串时，会给出轻提示而不是静默失败。
- 补充了独立的最小测试，覆盖解包识别、格式化输出、压缩输出和非法场景。

### 工具栏整理

- `压缩 / 下载 / 复制 / 导入` 改为 2x2 紧凑按钮布局，视觉风格更接近现有的分段小按钮。
- 保持原有按钮 id 和交互逻辑不变，只调整编排和尺寸，避免影响既有事件绑定。

## 1.0.2 - 2026-03-20

### 重点更新

- 树形预览的横向滚动与右侧路径固定逻辑重新收敛，避免修复过程中把滚动条越撑越长。
- 底部“当前节点”摘要重新接上完整长字符串补全，不再直接显示树列表里被截断过的预览文本。

### 树形预览与横向滚动

- 预览虚拟列表新增“当前片段最大横向内容宽度”基线，同一批可见行会共享这条宽度，短值行与长值行在横向滚动时不再出现右侧路径表现不一致的问题。
- 树形路径样式收敛为更稳定的 `sticky + 实色背景 + 左侧分隔线` 方案，移除了会干扰横向宽度计算的外溢装饰。
- 修复树形预览横向滚到最右侧时，路径与右边界之间会多出约 `10px` 空隙的问题。

### 当前节点摘要

- 当树形行里的字符串预览被截断为 `…` 时，底部摘要会异步向 worker 取该节点的完整值再回填显示。
- 底部摘要继续保持单行省略样式，但不再直接复用树列表那份已经被截断过的源码文本。

## 1.0.1 - 2026-03-20

### 重点更新

- 编辑区新增 `完整 / 精简` 双模式切换，大文本输入时可以直接切到更轻量的输入模式。
- 大文件场景补充了更明确的处理中反馈：完整模式粘贴大 JSON、精简模式切回完整模式时，编辑区会显示独立 loading 遮罩。
- 搜索功能继续增强：支持回车跳到下一个匹配、`Shift + Enter` 跳到上一个匹配，并补齐大数据下的搜索状态提示。
- 预览搜索性能优化：大文件 `搜预览` 改为延后调度，缓存结构化预览的可见行和命中集合，降低输入与“下一个匹配”时的整页卡顿。

### 预览与搜索体验

- 树形预览与文本预览共用命中结果，保持节点高亮、当前匹配定位和上下跳转的一致性。
- 树形与文本预览的可见区改为按缓存复用，避免仅切换当前命中时反复重建整棵可见树。
- 修复“展开全部 / 折叠全部”在结构化预览缓存开启后可能失效的问题，批量展开和折叠现在会先刷新缓存再重建预览。
- 搜索处理中会显示“搜索更新中 / 正在更新大文件搜索结果…”，并暂时禁用上下匹配按钮，避免用户误以为结果已经更新完毕。
- 搜索编辑区时不再顺带重绘整块预览无关内容；搜索预览时也不再无意义重绘编辑区高亮和行号。

### 大文件与性能

- 大文件解析节奏继续按文本规模动态延后，减少连续输入或一次性粘贴时的重复计算。
- 编辑区附属视图刷新拆成延后执行，优先让 loading 先进屏，降低“页面卡住但没有反馈”的体感问题。
- 树形预览底部保留“已经到底啦～”尾标，便于在超长数据下确认确实滚到了末尾。

### 界面与交互

- 顶部标题栏更新为 `design by Eko.Li`。
- 编辑区和预览区的搜索、复制、折叠、选择等交互继续做了细节修正，减少误触和状态不一致。
- 搜索、折叠与节点选择相关的轻提示和状态反馈继续补齐。

## 1.0.0 - 2026-03-18

### 初始版本

- 发布基于 Chrome Manifest V3 的离线 JSON 工作台。
- 提供格式化、压缩、树形预览、文本预览、元数据视图、下载、复制、导入、排序、节点复制、虚拟滚动和错误定位等核心能力。
